server:
  port: 8081

spring:
  application:
    name: flight-service


eureka:
  instance:
    # Force Eureka to use the IP address (127.0.0.1) instead of the Hostname
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/